<?php
/**
 * TripSalama - Driver Dashboard
 * Design Uber Premium
 */
?>

<style>
    .driver-dashboard {
        padding: var(--space-4);
        padding-bottom: calc(var(--nav-height) + var(--space-8));
    }

    /* Status Toggle - Main CTA */
    .driver-status-card {
        background: var(--uber-white);
        border-radius: var(--radius-2xl);
        padding: var(--space-6);
        box-shadow: var(--shadow-lg);
        margin-bottom: var(--space-6);
        text-align: center;
    }

    .driver-status-icon {
        width: 80px;
        height: 80px;
        margin: 0 auto var(--space-4);
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: var(--radius-full);
        transition: all 0.3s ease;
    }

    .driver-status-icon.online {
        background: var(--uber-green-soft);
        color: var(--uber-green);
    }

    .driver-status-icon.offline {
        background: var(--uber-gray-100);
        color: var(--uber-gray-500);
    }

    .driver-status-icon svg {
        width: 40px;
        height: 40px;
    }

    .driver-status-text {
        font-size: var(--text-2xl);
        font-weight: var(--font-bold);
        color: var(--uber-black);
        margin-bottom: var(--space-1);
    }

    .driver-status-hint {
        font-size: var(--text-sm);
        color: var(--uber-gray-500);
        margin-bottom: var(--space-4);
    }

    /* Toggle Switch */
    .status-toggle {
        position: relative;
        width: 80px;
        height: 44px;
        margin: 0 auto;
    }

    .status-toggle input {
        opacity: 0;
        width: 0;
        height: 0;
    }

    .status-toggle-slider {
        position: absolute;
        cursor: pointer;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: var(--uber-gray-300);
        border-radius: var(--radius-full);
        transition: background 0.3s ease;
    }

    .status-toggle-slider::before {
        position: absolute;
        content: '';
        height: 36px;
        width: 36px;
        left: 4px;
        bottom: 4px;
        background: var(--uber-white);
        border-radius: var(--radius-full);
        box-shadow: var(--shadow-md);
        transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    .status-toggle input:checked + .status-toggle-slider {
        background: var(--uber-green);
    }

    .status-toggle input:checked + .status-toggle-slider::before {
        transform: translateX(36px);
    }

    /* Stats */
    .driver-stats {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: var(--space-3);
        margin-bottom: var(--space-6);
    }

    .driver-stat {
        background: var(--uber-white);
        border-radius: var(--radius-xl);
        padding: var(--space-4);
        text-align: center;
        box-shadow: var(--shadow-xs);
    }

    .driver-stat-value {
        font-size: var(--text-2xl);
        font-weight: var(--font-bold);
        color: var(--uber-black);
        line-height: 1;
        margin-bottom: var(--space-1);
    }

    .driver-stat-label {
        font-size: 10px;
        color: var(--uber-gray-500);
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    /* Section Title */
    .section-title {
        font-size: var(--text-xs);
        font-weight: var(--font-semibold);
        color: var(--uber-gray-500);
        text-transform: uppercase;
        letter-spacing: 0.08em;
        margin-bottom: var(--space-4);
    }

    /* Ride Requests */
    .ride-requests {
        display: flex;
        flex-direction: column;
        gap: var(--space-3);
    }

    .ride-request {
        background: var(--uber-white);
        border-radius: var(--radius-xl);
        padding: var(--space-4);
        box-shadow: var(--shadow-sm);
    }

    .ride-request-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--space-3);
    }

    .ride-request-price {
        font-size: var(--text-xl);
        font-weight: var(--font-bold);
        color: var(--uber-green);
    }

    .ride-request-distance {
        font-size: var(--text-sm);
        color: var(--uber-gray-500);
    }

    .ride-request-route {
        display: flex;
        gap: var(--space-3);
        margin-bottom: var(--space-4);
    }

    .ride-request-dots {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0;
        padding: var(--space-1) 0;
    }

    .ride-request-dot {
        width: 10px;
        height: 10px;
        border-radius: var(--radius-full);
    }

    .ride-request-dot.pickup {
        background: var(--uber-green);
    }

    .ride-request-dot.dropoff {
        background: var(--uber-black);
    }

    .ride-request-line {
        width: 2px;
        height: 20px;
        background: var(--uber-gray-300);
    }

    .ride-request-addresses {
        flex: 1;
    }

    .ride-request-address {
        font-size: var(--text-sm);
        color: var(--uber-black);
        margin-bottom: var(--space-2);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .ride-request-address:last-child {
        margin-bottom: 0;
    }

    .ride-request-actions {
        display: flex;
        gap: var(--space-2);
    }

    .ride-request-btn {
        flex: 1;
        padding: var(--space-3);
        font-size: var(--text-sm);
        font-weight: var(--font-semibold);
        border: none;
        border-radius: var(--radius-lg);
        cursor: pointer;
        transition: all 0.2s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: var(--space-2);
    }

    .ride-request-btn.accept {
        background: var(--uber-black);
        color: var(--uber-white);
    }

    .ride-request-btn.accept:hover {
        background: var(--uber-gray-800);
    }

    .ride-request-btn.decline {
        background: var(--uber-gray-100);
        color: var(--uber-black);
    }

    .ride-request-btn.decline:hover {
        background: var(--uber-gray-200);
    }

    .ride-request-btn svg {
        width: 18px;
        height: 18px;
    }

    /* Empty State */
    .empty-state {
        text-align: center;
        padding: var(--space-10) var(--space-6);
        background: var(--uber-white);
        border-radius: var(--radius-xl);
    }

    .empty-state-icon {
        width: 64px;
        height: 64px;
        margin: 0 auto var(--space-4);
        color: var(--uber-gray-300);
    }

    .empty-state-icon svg {
        width: 100%;
        height: 100%;
    }

    .empty-state-title {
        font-size: var(--text-lg);
        font-weight: var(--font-bold);
        color: var(--uber-black);
        margin-bottom: var(--space-2);
    }

    .empty-state-text {
        font-size: var(--text-sm);
        color: var(--uber-gray-500);
    }

    /* Animation */
    @keyframes fadeInUp {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .driver-status-card { animation: fadeInUp 0.4s ease-out; }
    .driver-stats { animation: fadeInUp 0.4s ease-out 0.1s backwards; }
    .ride-requests { animation: fadeInUp 0.4s ease-out 0.2s backwards; }
</style>

<div class="driver-dashboard">
    <!-- Status Toggle -->
    <section class="driver-status-card">
        <div class="driver-status-icon <?= ($driver['is_available'] ?? false) ? 'online' : 'offline' ?>" id="statusIcon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <?php if ($driver['is_available'] ?? false): ?>
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                <polyline points="22 4 12 14.01 9 11.01"/>
                <?php else: ?>
                <circle cx="12" cy="12" r="10"/>
                <line x1="15" y1="9" x2="9" y2="15"/>
                <line x1="9" y1="9" x2="15" y2="15"/>
                <?php endif; ?>
            </svg>
        </div>
        <div class="driver-status-text" id="statusText">
            <?= ($driver['is_available'] ?? false) ? __('driver.online') : __('driver.offline') ?>
        </div>
        <div class="driver-status-hint">
            <?= ($driver['is_available'] ?? false) ? __('driver.accepting_rides') : __('driver.not_accepting') ?>
        </div>
        <label class="status-toggle">
            <input type="checkbox" id="availabilityToggle" <?= ($driver['is_available'] ?? false) ? 'checked' : '' ?>>
            <span class="status-toggle-slider"></span>
        </label>
    </section>

    <!-- Stats -->
    <section class="driver-stats">
        <div class="driver-stat">
            <div class="driver-stat-value"><?= $stats['today'] ?? 0 ?></div>
            <div class="driver-stat-label"><?= __('driver.today') ?></div>
        </div>
        <div class="driver-stat">
            <div class="driver-stat-value"><?= $stats['week'] ?? 0 ?></div>
            <div class="driver-stat-label"><?= __('driver.this_week') ?></div>
        </div>
        <div class="driver-stat">
            <div class="driver-stat-value"><?= number_format($stats['earnings'] ?? 0) ?></div>
            <div class="driver-stat-label"><?= __('driver.earnings') ?></div>
        </div>
    </section>

    <!-- Ride Requests -->
    <section>
        <h2 class="section-title"><?= __('driver.ride_requests') ?></h2>

        <?php if (!empty($pendingRides)): ?>
        <div class="ride-requests">
            <?php foreach ($pendingRides as $ride): ?>
            <div class="ride-request" data-ride-id="<?= $ride['id'] ?>">
                <div class="ride-request-header">
                    <span class="ride-request-price"><?= number_format($ride['estimated_price']) ?> MAD</span>
                    <span class="ride-request-distance"><?= number_format($ride['distance'], 1) ?> km</span>
                </div>
                <div class="ride-request-route">
                    <div class="ride-request-dots">
                        <div class="ride-request-dot pickup"></div>
                        <div class="ride-request-line"></div>
                        <div class="ride-request-dot dropoff"></div>
                    </div>
                    <div class="ride-request-addresses">
                        <div class="ride-request-address"><?= htmlspecialchars($ride['pickup_address']) ?></div>
                        <div class="ride-request-address"><?= htmlspecialchars($ride['dropoff_address']) ?></div>
                    </div>
                </div>
                <div class="ride-request-actions">
                    <button class="ride-request-btn decline" data-action="decline">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <line x1="18" y1="6" x2="6" y2="18"/>
                            <line x1="6" y1="6" x2="18" y2="18"/>
                        </svg>
                        <?= __('driver.decline') ?>
                    </button>
                    <button class="ride-request-btn accept" data-action="accept">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <polyline points="20 6 9 17 4 12"/>
                        </svg>
                        <?= __('driver.accept') ?>
                    </button>
                </div>
            </div>
            <?php endforeach; ?>
        </div>
        <?php else: ?>
        <div class="empty-state">
            <div class="empty-state-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <circle cx="12" cy="12" r="10"/>
                    <path d="M8 14s1.5 2 4 2 4-2 4-2"/>
                    <line x1="9" y1="9" x2="9.01" y2="9"/>
                    <line x1="15" y1="9" x2="15.01" y2="9"/>
                </svg>
            </div>
            <div class="empty-state-title"><?= __('driver.no_requests') ?></div>
            <div class="empty-state-text"><?= __('driver.wait_for_requests') ?></div>
        </div>
        <?php endif; ?>
    </section>
</div>

<script>
    // Toggle availability
    document.getElementById('availabilityToggle').addEventListener('change', async function() {
        const isAvailable = this.checked;
        const statusIcon = document.getElementById('statusIcon');
        const statusText = document.getElementById('statusText');

        try {
            const response = await ApiService.post('driver/availability', {
                is_available: isAvailable
            });

            if (response.success) {
                statusIcon.className = 'driver-status-icon ' + (isAvailable ? 'online' : 'offline');
                statusText.textContent = isAvailable
                    ? <?= json_encode(__('driver.online')) ?>
                    : <?= json_encode(__('driver.offline')) ?>;

                // Haptic feedback
                if ('vibrate' in navigator) {
                    navigator.vibrate(20);
                }
            }
        } catch (error) {
            console.error('Error updating availability:', error);
            this.checked = !isAvailable;
        }
    });
</script>
