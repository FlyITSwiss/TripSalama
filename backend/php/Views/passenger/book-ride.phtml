<?php
/**
 * TripSalama - Page Réservation
 * Layout Mobile avec carte Uber-like
 * Design System φ "Moroccan Luxury"
 */
?>

<div class="booking-page">
    <!-- Map Container -->
    <div class="booking-map-container">
        <div id="map" class="booking-map"></div>

        <!-- Location Button -->
        <button type="button" id="locateMeBtn" class="booking-locate-btn" title="<?= __('booking.my_location') ?>">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <circle cx="12" cy="12" r="3"/>
                <line x1="12" y1="2" x2="12" y2="6"/>
                <line x1="12" y1="18" x2="12" y2="22"/>
                <line x1="2" y1="12" x2="6" y2="12"/>
                <line x1="18" y1="12" x2="22" y2="12"/>
            </svg>
        </button>
    </div>

    <!-- Address Card -->
    <div class="booking-card">
        <form id="bookingForm" class="booking-form">
            <!-- Pickup -->
            <div class="booking-input-row">
                <div class="booking-indicator">
                    <span class="booking-dot pickup"></span>
                    <span class="booking-line"></span>
                </div>
                <div class="booking-input-wrapper">
                    <input type="text" id="pickupInput" class="booking-input"
                           placeholder="<?= __('booking.pickup_placeholder') ?>"
                           autocomplete="off">
                    <input type="hidden" id="pickupLat" name="pickup_lat">
                    <input type="hidden" id="pickupLng" name="pickup_lng">
                    <input type="hidden" id="pickupAddress" name="pickup_address">
                    <div id="pickupDropdown" class="booking-dropdown hidden"></div>
                    <!-- Indicateur de statut géolocalisation -->
                    <div id="pickupStatusIndicator" class="pickup-status hidden">
                        <span class="status-spinner"></span>
                        <span class="status-text"></span>
                    </div>
                </div>
            </div>

            <!-- Dropoff -->
            <div class="booking-input-row">
                <div class="booking-indicator">
                    <span class="booking-dot dropoff"></span>
                </div>
                <div class="booking-input-wrapper">
                    <input type="text" id="dropoffInput" class="booking-input"
                           placeholder="<?= __('booking.dropoff_placeholder') ?>"
                           autocomplete="off">
                    <input type="hidden" id="dropoffLat" name="dropoff_lat">
                    <input type="hidden" id="dropoffLng" name="dropoff_lng">
                    <input type="hidden" id="dropoffAddress" name="dropoff_address">
                    <div id="dropoffDropdown" class="booking-dropdown hidden"></div>
                </div>
            </div>
        </form>
    </div>

    <!-- Estimation Card (hidden by default) -->
    <div id="estimationCard" class="booking-card booking-estimation hidden">
        <div class="estimation-grid">
            <div class="estimation-item">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M12 22s-8-4.5-8-11.8A8 8 0 0 1 12 2a8 8 0 0 1 8 8.2c0 7.3-8 11.8-8 11.8z"/>
                    <circle cx="12" cy="10" r="3"/>
                </svg>
                <div>
                    <span class="estimation-label"><?= __('booking.distance') ?></span>
                    <span class="estimation-value" id="estimatedDistance">-</span>
                </div>
            </div>
            <div class="estimation-item">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <circle cx="12" cy="12" r="10"/>
                    <polyline points="12 6 12 12 16 14"/>
                </svg>
                <div>
                    <span class="estimation-label"><?= __('booking.duration') ?></span>
                    <span class="estimation-value" id="estimatedDuration">-</span>
                </div>
            </div>
        </div>
        <div class="estimation-price">
            <span class="price-label"><?= __('booking.price') ?></span>
            <span class="price-value" id="estimatedPrice">-</span>
        </div>
    </div>

    <!-- Confirm Button -->
    <button type="submit" form="bookingForm" id="confirmBtn" class="btn btn-accent btn-lg btn-block booking-confirm hidden">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
        </svg>
        <?= __('booking.confirm_ride') ?>
    </button>

    <!-- Quick Location Button -->
    <button type="button" id="quickLocateBtn" class="btn btn-secondary btn-lg btn-block booking-quick-locate">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <circle cx="12" cy="12" r="3"/>
        </svg>
        <?= __('booking.my_location') ?>
    </button>
</div>

<style>
/* ============================================
   BOOKING PAGE - MOBILE CONTAINER LAYOUT
   ============================================ */

/* Masquer l'attribution Leaflet (style Uber clean) */
.booking-map .leaflet-control-attribution {
    display: none !important;
}

.booking-page {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    padding-bottom: calc(var(--size-mobile-nav, 70px) + var(--space-8));
}

/* Map Container - Aspect Ratio φ */
.booking-map-container {
    position: relative;
    width: 100%;
    aspect-ratio: 1 / 1.2; /* Golden ratio inspired */
    border-radius: var(--radius-2xl);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    background: var(--stone-200);
}

.booking-map {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
}

/* Leaflet Controls */
.booking-map .leaflet-control-zoom {
    border: none !important;
    box-shadow: var(--shadow-md) !important;
    border-radius: var(--radius-lg) !important;
    overflow: hidden;
    margin: var(--space-3) !important;
}

.booking-map .leaflet-control-zoom a {
    width: 36px !important;
    height: 36px !important;
    line-height: 36px !important;
    font-size: 16px !important;
    color: var(--color-text) !important;
    background: var(--color-surface) !important;
    border: none !important;
}

.booking-map .leaflet-control-zoom a:hover {
    background: var(--stone-100) !important;
    color: var(--color-primary) !important;
}

/* Location Button on Map */
.booking-locate-btn {
    position: absolute;
    bottom: var(--space-4);
    right: var(--space-4);
    z-index: 1000;
    width: 48px;
    height: 48px;
    background: var(--color-surface);
    border: none;
    border-radius: var(--radius-full);
    box-shadow: var(--shadow-md);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    cursor: pointer;
    transition: var(--transition-all);
}

.booking-locate-btn:hover:not(.locating) {
    background: var(--color-primary);
    color: var(--color-text-inverse);
    transform: scale(1.08);
    box-shadow: var(--shadow-lg);
}

.booking-locate-btn svg {
    width: 24px;
    height: 24px;
    transition: transform 0.3s ease;
}

/* Animation pendant la géolocalisation */
.booking-locate-btn.locating {
    background: var(--color-primary);
    color: var(--color-text-inverse);
    cursor: wait;
    box-shadow: 0 0 0 4px var(--color-primary-soft), var(--shadow-md);
}

.booking-locate-btn.locating svg {
    animation: locatePulse 1.5s ease-in-out infinite;
}

@keyframes locatePulse {
    0%, 100% {
        transform: scale(1);
        opacity: 1;
    }
    50% {
        transform: scale(1.15);
        opacity: 0.7;
    }
}

/* Quick locate button animation */
.booking-quick-locate.locating {
    background: var(--color-primary);
    color: var(--color-text-inverse);
    cursor: wait;
}

.booking-quick-locate.locating svg {
    animation: locatePulse 1.5s ease-in-out infinite;
}

/* Address Card */
.booking-card {
    background: var(--color-surface);
    border-radius: var(--radius-xl);
    padding: var(--space-4);
    box-shadow: var(--shadow-md);
    border: 1px solid var(--color-border);
}

.booking-form {
    display: flex;
    flex-direction: column;
    gap: 0;
}

.booking-input-row {
    display: flex;
    align-items: stretch;
    gap: var(--space-3);
}

.booking-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--space-3) 0;
    width: 24px;
    flex-shrink: 0;
}

.booking-dot {
    width: 12px;
    height: 12px;
    border-radius: var(--radius-full);
    flex-shrink: 0;
}

.booking-dot.pickup {
    background: var(--success);
    box-shadow: 0 0 0 3px var(--success-soft);
    transition: box-shadow 0.3s ease;
}

/* Animation du dot pickup pendant la détection */
.booking-input-row:has(.pickup-status.detecting) .booking-dot.pickup {
    animation: dotPulseDetecting 1.2s ease-in-out infinite;
}

@keyframes dotPulseDetecting {
    0%, 100% {
        box-shadow: 0 0 0 3px var(--info-soft);
        background: var(--info);
    }
    50% {
        box-shadow: 0 0 0 6px var(--info-soft);
        background: var(--info);
    }
}

.booking-dot.dropoff {
    background: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(212, 168, 83, 0.2);
}

.booking-line {
    flex: 1;
    width: 2px;
    margin: var(--space-2) 0;
    background: repeating-linear-gradient(
        to bottom,
        var(--stone-300) 0,
        var(--stone-300) 4px,
        transparent 4px,
        transparent 8px
    );
}

.booking-input-wrapper {
    flex: 1;
    position: relative;
}

.booking-input {
    width: 100%;
    padding: var(--space-3) var(--space-4);
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--color-text);
    background: var(--stone-50);
    border: 2px solid transparent;
    border-radius: var(--radius-lg);
    transition: var(--transition-all);
}

.booking-input:focus {
    outline: none;
    background: var(--color-surface);
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px var(--color-primary-soft);
}

.booking-input::placeholder {
    color: var(--color-text-muted);
}

/* Dropdown */
.booking-dropdown {
    position: absolute;
    top: calc(100% + var(--space-2));
    left: 0;
    right: 0;
    background: var(--color-surface);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    max-height: 200px;
    overflow-y: auto;
    z-index: 1001;
}

/* Statut de géolocalisation */
.pickup-status {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-3);
    margin-top: var(--space-2);
    font-size: var(--text-sm);
    border-radius: var(--radius-md);
    transition: var(--transition-all);
}

.pickup-status.hidden {
    display: none;
}

.pickup-status.detecting {
    background: var(--info-soft);
    color: var(--info);
}

.pickup-status.found {
    background: var(--success-soft);
    color: var(--success);
}

.pickup-status.error {
    background: var(--error-soft);
    color: var(--error);
}

.pickup-status .status-icon {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
}

.pickup-status .status-text {
    flex: 1;
}

.pickup-status .status-accuracy {
    font-size: var(--text-xs);
    opacity: 0.8;
}

.pickup-status .status-spinner {
    width: 16px;
    height: 16px;
    border: 2px solid currentColor;
    border-top-color: transparent;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
    flex-shrink: 0;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

/* Option position actuelle dans dropdown */
.booking-dropdown-item.current-location {
    background: var(--success-soft);
    border-bottom: 1px solid var(--color-border);
}

.booking-dropdown-item.current-location:hover {
    background: var(--success-soft);
    filter: brightness(0.95);
}

.booking-dropdown-item,
.autocomplete-item {
    padding: var(--space-3) var(--space-4);
    display: flex;
    align-items: center;
    gap: var(--space-3);
    cursor: pointer;
    transition: var(--transition-colors);
    border-bottom: 1px solid var(--color-border-light);
}

.booking-dropdown-item:last-child,
.autocomplete-item:last-child {
    border-bottom: none;
}

.booking-dropdown-item:hover,
.autocomplete-item:hover {
    background: var(--stone-50);
}

/* Autocomplete item icon */
.autocomplete-item-icon {
    color: var(--color-primary);
    flex-shrink: 0;
}

/* Autocomplete item text container */
.autocomplete-item-text {
    flex: 1;
    min-width: 0;
    overflow: hidden;
}

.autocomplete-item-primary {
    font-size: var(--text-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-text);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.autocomplete-item-secondary {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    margin-top: 2px;
}

/* Estimation Card */
.booking-estimation {
    animation: fadeInUp var(--duration-normal) var(--ease-out-expo);
}

.estimation-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3);
    margin-bottom: var(--space-3);
}

.estimation-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3);
    background: var(--stone-50);
    border-radius: var(--radius-lg);
}

.estimation-item svg {
    width: 24px;
    height: 24px;
    color: var(--color-primary);
    flex-shrink: 0;
}

.estimation-item > div {
    display: flex;
    flex-direction: column;
}

.estimation-label {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.estimation-value {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text);
}

.estimation-price {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-4);
    background: linear-gradient(135deg, var(--emerald-50) 0%, var(--gold-50) 100%);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-primary-soft);
}

.price-label {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
}

.price-value {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-accent);
}

/* Buttons */
.booking-confirm {
    animation: fadeInUp var(--duration-normal) var(--ease-out-expo);
}

.booking-quick-locate {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
}

.booking-quick-locate svg {
    width: 22px;
    height: 22px;
}

/* Animation */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
</style>

<script>
    // Configuration pour le module booking
    window.BookingConfig = {
        apiUrl: AppConfig.apiPath,
        csrfToken: AppConfig.csrfToken,
        defaultCenter: [33.5731, -7.5898], // Casablanca, Morocco
        defaultZoom: 14,
        autoLocate: true, // Géolocalisation automatique activée
        i18n: {
            searching: <?= json_encode(__('ride.searching')) ?>,
            noResults: <?= json_encode(__('booking.no_results')) ?>,
            error: <?= json_encode(__('error.generic')) ?>,
            geolocationError: <?= json_encode(__('error.geolocation')) ?>,
            geolocationPermission: <?= json_encode(__('error.geolocation_permission')) ?>,
            geolocationUnavailable: <?= json_encode(__('error.geolocation_unavailable')) ?>,
            geolocationTimeout: <?= json_encode(__('error.geolocation_timeout')) ?>,
            detectingLocation: <?= json_encode(__('booking.detecting_location')) ?>,
            locationFound: <?= json_encode(__('booking.location_found')) ?>,
            changeLocation: <?= json_encode(__('booking.change_location')) ?>,
            useCurrentLocation: <?= json_encode(__('booking.use_current_location')) ?>,
            positionAccuracy: <?= json_encode(__('booking.position_accuracy')) ?>,
            confirmRide: <?= json_encode(__('booking.confirm_ride')) ?>,
            rideCreated: <?= json_encode(__('msg.ride_booked')) ?>
        }
    };
</script>
