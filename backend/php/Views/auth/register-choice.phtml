<!DOCTYPE html>
<html lang="<?= $_SESSION['lang'] ?? 'fr' ?>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="theme-color" content="#1B4D3E">
    <meta name="description" content="<?= __('app.tagline') ?>">
    <?= csrf_meta() ?>

    <title><?= __('auth.register') ?> - <?= __('app.name') ?></title>

    <link rel="icon" type="image/svg+xml" href="<?= asset_url('images/logo.svg') ?>">

    <!-- Design System CSS (includes Google Fonts) -->
    <link rel="stylesheet" href="<?= versioned_asset('css/tripsalama-design-system.css') ?>">
</head>
<body class="auth-body">
    <div class="auth-page">
        <!-- Moroccan Pattern Background -->
        <div class="auth-pattern"></div>

        <div class="auth-card glass-card animate-phi-enter">
            <!-- Logo & Header -->
            <div class="auth-header">
                <div class="auth-logo">
                    <img src="<?= asset_url('images/icons/icon.svg') ?>" alt="TripSalama" style="width: 80px; height: 80px; border-radius: var(--radius-xl);">
                </div>
                <h1 class="auth-title"><?= __('auth.create_account') ?></h1>
                <p class="auth-subtitle"><?= __('auth.register_as') ?></p>
            </div>

            <!-- Role Selection -->
            <div class="auth-form">
                <div class="role-selection">
                    <a href="<?= base_url('register/passenger') ?>" class="role-card glass-card">
                        <div class="role-icon passenger">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                                <circle cx="12" cy="7" r="4"/>
                            </svg>
                        </div>
                        <div class="role-content">
                            <h3 class="role-title"><?= __('auth.register_passenger') ?></h3>
                            <p class="role-description"><?= __('booking.title') ?></p>
                        </div>
                        <svg class="role-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <polyline points="9 18 15 12 9 6"/>
                        </svg>
                    </a>

                    <a href="<?= base_url('register/driver') ?>" class="role-card glass-card">
                        <div class="role-icon driver">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M5 17h14v-6h-4l-2-4H9l-2 4H5v6z"/>
                                <circle cx="7.5" cy="17" r="2"/>
                                <circle cx="16.5" cy="17" r="2"/>
                            </svg>
                        </div>
                        <div class="role-content">
                            <h3 class="role-title"><?= __('auth.register_driver') ?></h3>
                            <p class="role-description"><?= __('driver.title') ?></p>
                        </div>
                        <svg class="role-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <polyline points="9 18 15 12 9 6"/>
                        </svg>
                    </a>
                </div>
            </div>

            <!-- Footer -->
            <div class="auth-footer">
                <p class="auth-footer-text"><?= __('auth.have_account') ?></p>
                <a href="<?= base_url('login') ?>" class="btn btn-secondary btn-block">
                    <?= __('auth.login') ?>
                </a>
            </div>
        </div>

        <!-- Decorative Element -->
        <div class="auth-decoration">
            <svg viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg">
                <circle cx="100" cy="100" r="80" stroke="currentColor" stroke-width="0.5" opacity="0.3"/>
                <circle cx="100" cy="100" r="60" stroke="currentColor" stroke-width="0.5" opacity="0.2"/>
                <circle cx="100" cy="100" r="40" stroke="currentColor" stroke-width="0.5" opacity="0.1"/>
            </svg>
        </div>
    </div>
</body>
</html>
